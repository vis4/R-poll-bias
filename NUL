Das folgende Objekt ist maskiert from X (position 3):

    color, date, inst, party, value
Das folgende Objekt ist maskiert from X (position 4):

    color, date, inst, party, value
Das folgende Objekt ist maskiert from X (position 5):

    color, date, inst, party, value
Das folgende Objekt ist maskiert from X (position 6):

    color, date, inst, party, value
Das folgende Objekt ist maskiert from X (position 7):

    color, date, inst, party, value
Das folgende Objekt ist maskiert from X (position 8):

    color, date, inst, party, value
Das folgende Objekt ist maskiert from X (position 9):

    color, date, inst, party, value
Das folgende Objekt ist maskiert from X (position 10):

    color, date, inst, party, value
Das folgende Objekt ist maskiert from X (position 11):

    color, date, inst, party, value
Das folgende Objekt ist maskiert from X (position 12):

    color, date, inst, party, value
